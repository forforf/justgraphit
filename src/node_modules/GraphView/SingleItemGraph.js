
import React from 'react';

const SingleItemGraph = (props) =>
    <div>
      <h3>{props.graphName}</h3>
      Single Item Graph for {props.itemKey} -> {props.itemValue}
    </div>;

export default SingleItemGraph;
