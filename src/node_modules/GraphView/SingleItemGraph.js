
import React from 'react';
import moment from 'moment';

const style = {
  marginLeft: '5em',
  marginTop: '2em',
  paddingTop: '2em',
  paddingBottom: '2em',
  borderStyle: 'solid'
};

const SingleItemGraph = (props) =>
    <div style={style}>
      <div>Not enough to graph yet. Only a single lonely value of:</div>
      <h3>{props.itemNumber}</h3>
      {moment(props.itemDatetime).fromNow()}
    </div>;

export default SingleItemGraph;
